const ViewName = 'Three';
const HomePageName = 'Main';
const i18nSeparator = '.';
const logoTitle = 'Acro Admin';

const DzAlertOption = {
  themeOption: {
    primary: 'green',
    default: 'gray',
  },
};

const DzButtonOption = {
  themeOption: {
    primary: 'blue',
    default: 'gray',
  },
};

export const config = {
  // 当前业务配置
  ViewName,
  HomePageName,
  i18nSeparator,
  logoTitle,

  // 组件配置
  DzAlertOption,
  DzButtonOption,
};
